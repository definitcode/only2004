[opheld5,mithril_gauntlets] @drop_gauntlets;
[opheld5,adamant_gauntlets] @drop_gauntlets;
[opheld5,rune_gauntlets] @drop_gauntlets;
[opheld5,dragon_gauntlets] @drop_gauntlets;


[opheldu,bronze_tinderbox]
if (last_useitem = newbielogs) {
    @tut_light_logs_inv(last_useslot);
} else if (oc_param(last_useitem, unlit_arrow) = ^true) {
    @light_firearrow(last_useitem);
} else if (oc_category(last_useitem) = category_22) {
    @light_logs_inv(last_useslot);
} else if (oc_category(last_useitem) = light_source_unlit) {
    @ignite_light_source(last_useitem);
} else {
    ~displaymessage(^dm_default);
}

[opheldu,iron_tinderbox]
if (last_useitem = newbielogs) {
    @tut_light_logs_inv(last_useslot);
} else if (oc_param(last_useitem, unlit_arrow) = ^true) {
    @light_firearrow(last_useitem);
} else if (oc_category(last_useitem) = category_22) {
    @light_logs_inv(last_useslot);
} else if (oc_category(last_useitem) = light_source_unlit) {
    @ignite_light_source(last_useitem);
} else {
    ~displaymessage(^dm_default);
}

[opheldu,steel_tinderbox]
if (last_useitem = newbielogs) {
    @tut_light_logs_inv(last_useslot);
} else if (oc_param(last_useitem, unlit_arrow) = ^true) {
    @light_firearrow(last_useitem);
} else if (oc_category(last_useitem) = category_22) {
    @light_logs_inv(last_useslot);
} else if (oc_category(last_useitem) = light_source_unlit) {
    @ignite_light_source(last_useitem);
} else {
    ~displaymessage(^dm_default);
}

[opheldu,mithril_tinderbox]
if (last_useitem = newbielogs) {
    @tut_light_logs_inv(last_useslot);
} else if (oc_param(last_useitem, unlit_arrow) = ^true) {
    @light_firearrow(last_useitem);
} else if (oc_category(last_useitem) = category_22) {
    @light_logs_inv(last_useslot);
} else if (oc_category(last_useitem) = light_source_unlit) {
    @ignite_light_source(last_useitem);
} else {
    ~displaymessage(^dm_default);
}

[opheldu,adamant_tinderbox]
if (last_useitem = newbielogs) {
    @tut_light_logs_inv(last_useslot);
} else if (oc_param(last_useitem, unlit_arrow) = ^true) {
    @light_firearrow(last_useitem);
} else if (oc_category(last_useitem) = category_22) {
    @light_logs_inv(last_useslot);
} else if (oc_category(last_useitem) = light_source_unlit) {
    @ignite_light_source(last_useitem);
} else {
    ~displaymessage(^dm_default);
}

[opheldu,dragon_tinderbox]
if (last_useitem = newbielogs) {
    @tut_light_logs_inv(last_useslot);
} else if (oc_param(last_useitem, unlit_arrow) = ^true) {
    @light_firearrow(last_useitem);
} else if (oc_category(last_useitem) = category_22) {
    @light_logs_inv(last_useslot);
} else if (oc_category(last_useitem) = light_source_unlit) {
    @ignite_light_source(last_useitem);
} else {
    ~displaymessage(^dm_default);
}


[opheldu,rune_tinderbox]
if (last_useitem = newbielogs) {
    @tut_light_logs_inv(last_useslot);
} else if (oc_param(last_useitem, unlit_arrow) = ^true) {
    @light_firearrow(last_useitem);
} else if (oc_category(last_useitem) = category_22) {
    @light_logs_inv(last_useslot);
} else if (oc_category(last_useitem) = light_source_unlit) {
    @ignite_light_source(last_useitem);
} else {
    ~displaymessage(^dm_default);
}

[opheldu,founders_tinderbox]
if (last_useitem = newbielogs) {
    @tut_light_logs_inv(last_useslot);
} else if (oc_param(last_useitem, unlit_arrow) = ^true) {
    @light_firearrow(last_useitem);
} else if (oc_category(last_useitem) = category_22) {
    @light_logs_inv(last_useslot);
} else if (oc_category(last_useitem) = light_source_unlit) {
    @ignite_light_source(last_useitem);
} else {
    ~displaymessage(^dm_default);
}


// Chisels

[opheldu,bronze_chisel]
switch_obj (last_useitem)
{
    case uncut_sapphire, uncut_emerald, uncut_ruby, uncut_diamond, uncut_dragonstone, uncut_opal, uncut_jade, uncut_red_topaz : @crafting_gem(last_useitem);
    case opal, smalloysterpearls, bigoysterpearls, sapphire, emerald, ruby, diamond, dragonstone : @make_bolt_tips(last_useitem);
    case wolf_bones : @make_wolf_bone_tips;
    case zqbonebeads : @craft_ivorybeads;
    case default : ~displaymessage(^dm_default);
};

[opheldu,iron_chisel]
switch_obj (last_useitem)
{
    case uncut_sapphire, uncut_emerald, uncut_ruby, uncut_diamond, uncut_dragonstone, uncut_opal, uncut_jade, uncut_red_topaz : @crafting_gem(last_useitem);
    case opal, smalloysterpearls, bigoysterpearls, sapphire, emerald, ruby, diamond, dragonstone : @make_bolt_tips(last_useitem);
    case wolf_bones : @make_wolf_bone_tips;
    case zqbonebeads : @craft_ivorybeads;
    case default : ~displaymessage(^dm_default);
};

[opheldu,steel_chisel]
switch_obj (last_useitem)
{
    case uncut_sapphire, uncut_emerald, uncut_ruby, uncut_diamond, uncut_dragonstone, uncut_opal, uncut_jade, uncut_red_topaz : @crafting_gem(last_useitem);
    case opal, smalloysterpearls, bigoysterpearls, sapphire, emerald, ruby, diamond, dragonstone : @make_bolt_tips(last_useitem);
    case wolf_bones : @make_wolf_bone_tips;
    case zqbonebeads : @craft_ivorybeads;
    case default : ~displaymessage(^dm_default);
};

[opheldu,mithril_chisel]
switch_obj (last_useitem)
{
    case uncut_sapphire, uncut_emerald, uncut_ruby, uncut_diamond, uncut_dragonstone, uncut_opal, uncut_jade, uncut_red_topaz : @crafting_gem(last_useitem);
    case opal, smalloysterpearls, bigoysterpearls, sapphire, emerald, ruby, diamond, dragonstone : @make_bolt_tips(last_useitem);
    case wolf_bones : @make_wolf_bone_tips;
    case zqbonebeads : @craft_ivorybeads;
    case default : ~displaymessage(^dm_default);
};

[opheldu,adamant_chisel]
switch_obj (last_useitem)
{
    case uncut_sapphire, uncut_emerald, uncut_ruby, uncut_diamond, uncut_dragonstone, uncut_opal, uncut_jade, uncut_red_topaz : @crafting_gem(last_useitem);
    case opal, smalloysterpearls, bigoysterpearls, sapphire, emerald, ruby, diamond, dragonstone : @make_bolt_tips(last_useitem);
    case wolf_bones : @make_wolf_bone_tips;
    case zqbonebeads : @craft_ivorybeads;
    case default : ~displaymessage(^dm_default);
};

[opheldu,rune_chisel]
switch_obj (last_useitem)
{
    case uncut_sapphire, uncut_emerald, uncut_ruby, uncut_diamond, uncut_dragonstone, uncut_opal, uncut_jade, uncut_red_topaz : @crafting_gem(last_useitem);
    case opal, smalloysterpearls, bigoysterpearls, sapphire, emerald, ruby, diamond, dragonstone : @make_bolt_tips(last_useitem);
    case wolf_bones : @make_wolf_bone_tips;
    case zqbonebeads : @craft_ivorybeads;
    case default : ~displaymessage(^dm_default);
};

[opheldu,dragon_chisel]
switch_obj (last_useitem)
{
    case uncut_sapphire, uncut_emerald, uncut_ruby, uncut_diamond, uncut_dragonstone, uncut_opal, uncut_jade, uncut_red_topaz : @crafting_gem(last_useitem);
    case opal, smalloysterpearls, bigoysterpearls, sapphire, emerald, ruby, diamond, dragonstone : @make_bolt_tips(last_useitem);
    case wolf_bones : @make_wolf_bone_tips;
    case zqbonebeads : @craft_ivorybeads;
    case default : ~displaymessage(^dm_default);
};
